services:
  iperf3-fleet:
    build: .
    image: iperf3-fleet:latest
    container_name: iperf3-fleet
    restart: unless-stopped
    ports:
      - "8088:8000"
    environment:
      TZ: Asia/Shanghai
      DATA_DIR: /data
    volumes:
      - ./data:/data
